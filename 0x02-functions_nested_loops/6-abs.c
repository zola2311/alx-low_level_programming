#include "main.h"
/**
 *_abs - Computes the absolute value of an integer
 *Description: number being tested
 *@n:integer n
 *Return: Always 0.
 */
int _abs(int n)
{
if (n < 0)
{
return (-n);
}
else
{
return (n);
}
return (0);
}
